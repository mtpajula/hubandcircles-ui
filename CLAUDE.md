# hubandcircles-ui (repo B)

Vue 3 + Vite + MapLibre frontend. Presentation only, no computation.
Follow `.claude/skills/ui-dev/SKILL.md`. Architecture: `../ARKKITEHTUURI.md`
(Finnish prose, English identifiers). Visual spec: `UI-SPEC.md` (binding).

All code, comments, identifiers, i18n keys and commit messages are English (P10).

Done: `npm run typecheck && npm run lint && npm test && npm run build`
